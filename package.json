{
  "scripts": {
    "start": "npm run generate  && npm run test",
    "generate": "node parser.js  ./samplecode.js",
    "test": "jest  --coverage"
  },
  "devDependencies": {
    "jest": "^27.5.1"
  }
}

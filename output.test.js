const Rectangle = require('./samplecode') 
 test('constructor', () => {  
		let rectangle= new Rectangle(1, 2); 

			expect(rectangle.height).toBe(1);  
			expect(rectangle.width).toBe(2); 
		});
		 
 test("getHeight", () => {
            let rectangle = new Rectangle(1, 2);
            expect(rectangle.getHeight()).toBe(1);
        });;
         
test('setWeight', () => {  
		let rectangle= new Rectangle(1,1); 
            rectangle.setWeight(21)
			expect(rectangle.height).toBe(21);  
		});
		const Rectangle = require('./samplecode') 
 test('constructor', () => {  
		let rectangle= new Rectangle(1, 2); 

			expect(rectangle.height).toBe(1);  
			expect(rectangle.width).toBe(2); 
		});
		 
 test("getHeight", () => {
            let rectangle = new Rectangle(1, 2);
            expect(rectangle.getHeight()).toBe(1);
        });;
         
test('setWeight', () => {  
		let rectangle= new Rectangle(1,1); 
            rectangle.setWeight(24)
			expect(rectangle.height).toBe(24);  
		});
		const Rectangle = require('./samplecode') 
 test('constructor', () => {  
		let rectangle= new Rectangle(1, 2); 

			expect(rectangle.height).toBe(1);  
			expect(rectangle.width).toBe(2); 
		});
		 
 test("getHeight", () => {
            let rectangle = new Rectangle(1, 2);
            expect(rectangle.getHeight()).toBe(1);
        });;
         
test('setWeight', () => {  
		let rectangle= new Rectangle(1,1); 
            rectangle.setWeight(77)
			expect(rectangle.height).toBe(77);  
		});
		const Rectangle = require('./samplecode') 
 test('constructor', () => {  
		let rectangle= new Rectangle(1, 2); 

			expect(rectangle.height).toBe(1);  
			expect(rectangle.width).toBe(2); 
		});
		 
 test("getHeight", () => {
            let rectangle = new Rectangle(1, 2);
            expect(rectangle.getHeight()).toBe(1);
        });;
         
test('setWeight', () => {  
		let rectangle= new Rectangle(1,1); 
            rectangle.setWeight(88)
			expect(rectangle.height).toBe(88);  
		});
		const Rectangle = require('./samplecode') 
 test('constructor', () => {  
		let rectangle= new Rectangle(1, 2); 

			expect(rectangle.height).toBe(1);  
			expect(rectangle.width).toBe(2); 
		});
		 
 test("getHeight", () => {
            let rectangle = new Rectangle(1, 2);
            expect(rectangle.getHeight()).toBe(1);
        });;
         
test('setWeight', () => {  
		let rectangle= new Rectangle(1,1); 
            rectangle.setWeight(89)
			expect(rectangle.height).toBe(89);  
		});
		const Rectangle = require('./samplecode') 
 test('constructor', () => {  
		let rectangle= new Rectangle(1, 2); 

			expect(rectangle.height).toBe(1);  
			expect(rectangle.width).toBe(2); 
		});
		 
 test("getHeight", () => {
            let rectangle = new Rectangle(1, 2);
            expect(rectangle.getHeight()).toBe(1);
        });;
         
test('setWeight', () => {  
		let rectangle= new Rectangle(1,1); 
            rectangle.setWeight(6)
			expect(rectangle.height).toBe(6);  
		});
		const Rectangle = require('./samplecode') 
 test('constructor', () => {  
		let rectangle= new Rectangle(1, 2); 

			expect(rectangle.height).toBe(1);  
			expect(rectangle.width).toBe(2); 
		});
		 
 test("getHeight", () => {
            let rectangle = new Rectangle(1, 2);
            expect(rectangle.getHeight()).toBe(1);
        });;
         
test('setWeight', () => {  
		let rectangle= new Rectangle(1,1); 
            rectangle.setWeight(99)
			expect(rectangle.height).toBe(99);  
		});
		const Rectangle = require('./samplecode') 
 test('constructor', () => {  
		let rectangle= new Rectangle(1, 2); 

			expect(rectangle.height).toBe(1);  
			expect(rectangle.width).toBe(2); 
		});
		 
 test("getHeight", () => {
            let rectangle = new Rectangle(1, 2);
            expect(rectangle.getHeight()).toBe(1);
        });;
         
test('setWeight', () => {  
		let rectangle= new Rectangle(1,1); 
            rectangle.setWeight(55)
			expect(rectangle.height).toBe(55);  
		});
		const Rectangle = require('./samplecode') 
 test('constructor', () => {  
		let rectangle= new Rectangle(1, 2); 

			expect(rectangle.height).toBe(1);  
			expect(rectangle.width).toBe(2); 
		});
		 
 test("getHeight", () => {
            let rectangle = new Rectangle(1, 2);
            expect(rectangle.getHeight()).toBe(1);
        });;
         
test('setWeight', () => {  
		let rectangle= new Rectangle(1,1); 
            rectangle.setWeight(73)
			expect(rectangle.height).toBe(73);  
		});
		const Rectangle = require('./samplecode') 
 test('constructor', () => {  
		let rectangle= new Rectangle(1, 2); 

			expect(rectangle.height).toBe(1);  
			expect(rectangle.width).toBe(2); 
		});
		 
 test("getHeight", () => {
            let rectangle = new Rectangle(1, 2);
            expect(rectangle.getHeight()).toBe(1);
        });;
         
test('setWeight', () => {  
		let rectangle= new Rectangle(1,1); 
            rectangle.setWeight(56)
			expect(rectangle.height).toBe(56);  
		});
		const Rectangle = require('./samplecode') 
 test('constructor', () => {  
		let rectangle= new Rectangle(1, 2); 

			expect(rectangle.height).toBe(1);  
			expect(rectangle.width).toBe(2); 
		});
		 
test('setWeight', () => {  
		let rectangle= new Rectangle(1,1); 
            rectangle.setWeight(88)
			expect(rectangle.height).toBe(88);  
		});
		 
 test("getHeight", () => {
            let rectangle = new Rectangle(1, 2);
            expect(rectangle.getHeight()).toBe(1);
        });;
        const Rectangle = require('./samplecode') 
 test('constructor', () => {  
		let rectangle= new Rectangle(1, 2); 

			expect(rectangle.height).toBe(1);  
			expect(rectangle.width).toBe(2); 
		});
		 
 test("getHeight", () => {
            let rectangle = new Rectangle(1, 2);
            expect(rectangle.getHeight()).toBe(1);
        });;
         
test('setWeight', () => {  
		let rectangle= new Rectangle(1,1); 
            rectangle.setWeight(56)
			expect(rectangle.height).toBe(56);  
		});
		const Rectangle = require('./samplecode') 
 test('constructor', () => {  
		let rectangle= new Rectangle(1, 2); 

			expect(rectangle.height).toBe(1);  
			expect(rectangle.width).toBe(2); 
		});
		 
 test("getHeight", () => {
            let rectangle = new Rectangle(1, 2);
            expect(rectangle.getHeight()).toBe(1);
        });;
         
test('setWeight', () => {  
		let rectangle= new Rectangle(1,1); 
            rectangle.setWeight(95)
			expect(rectangle.height).toBe(95);  
		});
		const Rectangle = require('./samplecode') 
 test('constructor', () => {  
		let rectangle= new Rectangle(1, 2); 

			expect(rectangle.height).toBe(1);  
			expect(rectangle.width).toBe(2); 
		});
		 
 test("getHeight", () => {
            let rectangle = new Rectangle(1, 2);
            expect(rectangle.getHeight()).toBe(1);
        });;
         
test('setWeight', () => {  
		let rectangle= new Rectangle(1,1); 
            rectangle.setWeight(1)
			expect(rectangle.height).toBe(1);  
		});
		const Rectangle = require('./samplecode') 
 test('constructor', () => {  
		let rectangle= new Rectangle(1, 2); 

			expect(rectangle.height).toBe(1);  
			expect(rectangle.width).toBe(2); 
		});
		 
 test("getHeight", () => {
            let rectangle = new Rectangle(1, 2);
            expect(rectangle.getHeight()).toBe(1);
        });;
         
test('setWeight', () => {  
		let rectangle= new Rectangle(1,1); 
            rectangle.setWeight(20)
			expect(rectangle.height).toBe(20);  
		});
		 
 test("getHeight", () => {
            let rectangle = new Rectangle(1, 2);
            expect(rectangle.getHeight()).toBe(1);
        });;
        const Rectangle = require('./samplecode') 
 test('constructor', () => {  
		let rectangle= new Rectangle(1, 2); 

			expect(rectangle.height).toBe(1);  
			expect(rectangle.width).toBe(2); 
		});
		 
test('setWeight', () => {  
		let rectangle= new Rectangle(1,1); 
            rectangle.setWeight(20)
			expect(rectangle.height).toBe(20);  
		});
		const Rectangle = require('./samplecode') 
 test('constructor', () => {  
		let rectangle= new Rectangle(1, 2); 

			expect(rectangle.height).toBe(1);  
			expect(rectangle.width).toBe(2); 
		});
		 
 test("getHeight", () => {
            let rectangle = new Rectangle(1, 2);
            expect(rectangle.getHeight()).toBe(1);
        });;
         
test('setWeight', () => {  
		let rectangle= new Rectangle(1,1); 
            rectangle.setWeight(68)
			expect(rectangle.height).toBe(68);  
		});
		const Rectangle = require('./samplecode') 
 test('constructor', () => {  
		let rectangle= new Rectangle(1, 2); 

			expect(rectangle.height).toBe(1);  
			expect(rectangle.width).toBe(2); 
		});
		 
 test("getHeight", () => {
            let rectangle = new Rectangle(1, 2);
            expect(rectangle.getHeight()).toBe(1);
        });;
         
test('setWeight', () => {  
		let rectangle= new Rectangle(1,1); 
            rectangle.setWeight(96)
			expect(rectangle.height).toBe(96);  
		});
		const Rectangle = require('./samplecode') 
 test('constructor', () => {  
		let rectangle= new Rectangle(1, 2); 

			expect(rectangle.height).toBe(1);  
			expect(rectangle.width).toBe(2); 
		});
		 
 test("getHeight", () => {
            let rectangle = new Rectangle(1, 2);
            expect(rectangle.getHeight()).toBe(1);
        });;
         
test('setWeight', () => {  
		let rectangle= new Rectangle(1,1); 
            rectangle.setWeight(37)
			expect(rectangle.height).toBe(37);  
		});
		const Rectangle = require('./samplecode') 
 test('constructor', () => {  
		let rectangle= new Rectangle(1, 2); 

			expect(rectangle.height).toBe(1);  
			expect(rectangle.width).toBe(2); 
		});
		 
 test("getHeight", () => {
            let rectangle = new Rectangle(1, 2);
            expect(rectangle.getHeight()).toBe(1);
        });;
         
test('setWeight', () => {  
		let rectangle= new Rectangle(1,1); 
            rectangle.setWeight(25)
			expect(rectangle.height).toBe(25);  
		});
		 
 test("getHeight", () => {
            let rectangle = new Rectangle(1, 2);
            expect(rectangle.getHeight()).toBe(1);
        });;
        const Rectangle = require('./samplecode') 
 test('constructor', () => {  
		let rectangle= new Rectangle(1, 2); 

			expect(rectangle.height).toBe(1);  
			expect(rectangle.width).toBe(2); 
		});
		 
test('setWeight', () => {  
		let rectangle= new Rectangle(1,1); 
            rectangle.setWeight(56)
			expect(rectangle.height).toBe(56);  
		});
		 
 test("getHeight", () => {
            let rectangle = new Rectangle(1, 2);
            expect(rectangle.getHeight()).toBe(1);
        });;
        const Rectangle = require('./samplecode') 
 test('constructor', () => {  
		let rectangle= new Rectangle(1, 2); 

			expect(rectangle.height).toBe(1);  
			expect(rectangle.width).toBe(2); 
		});
		 
test('setWeight', () => {  
		let rectangle= new Rectangle(1,1); 
            rectangle.setWeight(33)
			expect(rectangle.height).toBe(33);  
		});
		const Rectangle = require('./samplecode') 
 test('constructor', () => {  
		let rectangle= new Rectangle(1, 2); 

			expect(rectangle.height).toBe(1);  
			expect(rectangle.width).toBe(2); 
		});
		 
 test("getHeight", () => {
            let rectangle = new Rectangle(1, 2);
            expect(rectangle.getHeight()).toBe(1);
        });;
         
test('setWeight', () => {  
		let rectangle= new Rectangle(1,1); 
            rectangle.setWeight(92)
			expect(rectangle.height).toBe(92);  
		});
		 
 test("getHeight", () => {
            let rectangle = new Rectangle(1, 2);
            expect(rectangle.getHeight()).toBe(1);
        });;
        const Rectangle = require('./samplecode') 
 test('constructor', () => {  
		let rectangle= new Rectangle(1, 2); 

			expect(rectangle.height).toBe(1);  
			expect(rectangle.width).toBe(2); 
		});
		 
test('setWeight', () => {  
		let rectangle= new Rectangle(1,1); 
            rectangle.setWeight(14)
			expect(rectangle.height).toBe(14);  
		});
		